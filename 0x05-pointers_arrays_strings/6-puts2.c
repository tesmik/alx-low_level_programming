#include "main.h"
/**
 *puts2 - reverses a string
 *@str: string being reversed
 */
void puts2(char *str)
{
int i;
i = 0;

while (s[i] != '\0')
{
i++;
}
x = 0;
while (x < y)
{
if ((i % 2) == 0)
_putchar(str[a]);
a++;
}
_putchar('\n');
}
