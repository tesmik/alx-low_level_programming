#include "main.h"
/**
 *puts2 - reverses a string
 *@str: string being reversed
 */
void puts2(char *str)
{
int i;
i = 0;

while (str[i] != '\0')
{
if ((i % 2) == 0)
_putchar(str[i]);
a++;
}
_putchar('\n');
}
